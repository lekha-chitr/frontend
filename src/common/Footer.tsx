import * as React from 'react';

interface IProps {
  name: string;
}

class Footer extends React.Component {
  render() {
    return (
      <div>
        <h1>Footer</h1>
      </div>
    );
  }
}

export default Footer;
